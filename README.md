# Trie-based-Database-Storage

## Data
To generate IPv4 addr list run the following script
```bash
# python3 gen_ip_data.py <number of ipv4 to generate>

python3 gen_ip_data.py 100 # generates 100 IPv4 addresses
```
